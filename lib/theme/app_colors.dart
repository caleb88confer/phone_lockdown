import 'package:flutter/material.dart';

class AppColors {
  static const blockingBackground = Color(0xFF3D1111);
  static const nonBlockingBackground = Color(0xFF113D17);
  static const profileSectionBackground = Color(0xFF1E1E1E);
  static const selectedProfile = Color(0x4D2196F3); // blue with 30% opacity
  static const selectedProfileBorder = Colors.blue;
}
